# REDIT backend

Python source: `src/redit/`

**Setup and commands** run from the **project root** (`REDIT/`):

```bash
uv sync
uv run uvicorn redit.main:app --reload
```

See **[FILE-GUIDE.md](./FILE-GUIDE.md)** for a short description of every backend file.
